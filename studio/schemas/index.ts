import blockContent from './blockContent'
import post from './post'
import category from './category'
import page from './page'
import hero from './blocks/hero'

export const schemaTypes = [post, blockContent, category, page, hero]
